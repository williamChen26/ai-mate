{
  "name": "@production-spec-graph/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm run build && node dist/src/main.js",
    "smoke": "pnpm run build && node dist/scripts/smoke-sync-server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@fastify/websocket": "^11.0.2",
    "@tldraw/sync-core": "5.0.1",
    "fastify": "^5.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "ws": "^8.18.1"
  }
}
