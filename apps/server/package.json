{
  "name": "@production-spec-graph/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm --filter mate build && pnpm run build && node dist/src/main.js",
    "smoke": "pnpm --filter mate build && pnpm run build && node dist/scripts/smoke-sync-server.js",
    "test": "pnpm --filter mate build && vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@production-spec-graph/shared": "workspace:*",
    "@fastify/websocket": "^11.0.2",
    "@tldraw/sync-core": "5.0.1",
    "fastify": "^5.3.0",
    "mate": "workspace:*",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "@types/ws": "^8.18.1",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "ws": "^8.18.1"
  }
}
