# Feedback: Sprint 1 Round 1

## Must Fix (blocks approval)
1. Required commands do not run — `pnpm --filter @production-spec-graph/graph-protocol test`, `build`, and `schema:export` all fail because `vitest`, `tsc`, and `tsx` are unavailable; restore dependency installation or a valid offline cache, then rerun and record passing evidence. (AC-1.1, AC-1.2, AC-1.6)
2. Focused behavior tests are not executable — validation and JSON Schema tests exist but have not run, so valid graph acceptance, invalid graph rejection, inspectable issue data, and schema export behavior remain unverified. (AC-1.3, AC-1.4, AC-1.5)
3. Selected TDD evidence is incomplete — the build log records no captured RED, no GREEN, and no refactor-green run; after dependencies work, run the focused tests through the TDD stages or clearly document the recovered evidence in the next build log. (TDD gate, AC-1.3, AC-1.4, AC-1.5)
4. JSON Schema export command does not execute — the checked-in artifact parses, but `schema:export` fails before running `src/export-schema.ts`, so the package cannot yet prove it can regenerate the machine-readable schema. (AC-1.5, AC-1.6)

## Should Fix (won't block but noted)
1. Keep the next build log explicit about whether dependency installation used live registry access, a cache, or another local workaround so the evaluator can reproduce the commands.
2. If `pnpm-lock.yaml` is produced during the fix, ensure it is generated by a successful install and included intentionally as workspace metadata.

## Won't Fix (acceptable tradeoffs)
1. No browser E2E is needed for Sprint 1 because the approved contract has no UI surface and uses package-level runtime verification instead.
